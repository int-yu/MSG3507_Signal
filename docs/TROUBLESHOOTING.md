# 现场排障

- 幅值偏低：检查窗相干增益、FFT 缩放、ADC偏置和前端分压。
- 频率跳动：增加记录长度/迟滞，检查过零斜率和计时器时钟。
- 相位漂移：确认同步采样、参考频率一致且 NCO 未清相位。
- I/Q 二倍频：减小 alpha 或提高载波/低通截止比。
- LMS 发散：降低 mu、启用 NLMS、检查参考通道方向。
- 饱和计数增长：降低增益或增加 SOS 缩放，禁止只清计数。
- FFT 返回 ERANGE：CMSIS 宏未定义或长度不是 256/512。
- MATHACL：首版隔离适配边界，硬件事务需按 SDK 真机实测。